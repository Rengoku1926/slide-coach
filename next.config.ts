import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'assets.aceternity.com', 'user-images.githubusercontent.com'],
  },
};

export default nextConfig;
